{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "aca.serviceAccountName" . }}
  labels:
    {{- include "aca.labels" . | nindent 4 }}
{{- end }}
