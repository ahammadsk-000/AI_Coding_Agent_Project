================================================================================
AI CODING AGENT PLATFORM — CONVERSATION COMPACT SUMMARY
================================================================================

1. PRIMARY REQUEST AND INTENT
--------------------------------------------------------------------------------
The user is building an "AI Coding Agent Platform" — an open, self-hostable
Cursor/OpenHands/Devin-style platform — and has been progressively implementing
its 10-phase roadmap. Across this conversation the user requested (in order):
fix Docker Desktop/WSL startup, fix frontend build, fix backend startup errors,
debug repository ingestion, add a Files tab with syntax-highlighted chunk
preview, then implement Phases 3->10 sequentially ("implement from phase 3 to
phase 10... if need modification as part of adding new phases then modify code
if necessary"). The user explicitly accepted bounded "v1" slices for large
phases and deferral of impossible-to-test-locally parts. Most recently:
"i feel everything is working correct, now it's time to move to the Phase 10"
— then the user REJECTED/interrupted my AskUserQuestion that proposed 4 scope
options for Phase 10, and requested this summary. Phase 10 (Enterprise:
multi-tenant orgs, fine-grained RBAC, SSO/SCIM, billing, plugin marketplace)
has NOT been started.

2. KEY TECHNICAL CONCEPTS
--------------------------------------------------------------------------------
- Monorepo: apps/api (FastAPI, Python 3.12, async SQLAlchemy, Alembic) +
  apps/web (React 18 + Vite + TypeScript + Tailwind + Zustand + React Query)
- Infra (docker-compose): postgres:16, redis:7, qdrant:v1.11.3, Celery worker,
  Flower, nginx, Prometheus, Grafana
- Domain-driven structure: app/domain/{auth,users,repositories,search,chat,
  memory,sandbox,github}, app/infrastructure/{db,redis,qdrant,embeddings,
  parsers,git,llm,sandbox,github}, app/api/v1/
- RAG: hybrid retrieval (Qdrant dense + Postgres BM25 tsvector), Reciprocal
  Rank Fusion, cross-encoder reranker (ms-marco-MiniLM-L-6-v2), embeddings
  BAAI/bge-small-en-v1.5
- LLM abstraction: Ollama (local, default llama3.2:latest) + OpenAI providers,
  WebSocket token streaming
- Prometheus multiprocess metrics for the Celery worker; Grafana provisioning
- Helm chart with HPA/PDB/NetworkPolicy/Ingress; validated via helm-in-Docker +
  kubeconform; deployed live to Docker Desktop's kind-based Kubernetes via a
  local registry
- Docker-socket-based sandbox (sibling containers, network=none, cap_drop ALL,
  read-only root, non-root, resource caps, timeout)
- GitHub PAT-based REST integration (PR creation + AI review)
- Alembic migrations: 0001_initial, 0002_repositories, 0003_search, 0004_chat,
  0005_memory

3. FILES AND CODE SECTIONS
--------------------------------------------------------------------------------
PHASE 5 — SANDBOX (most recently completed before Phase 6):
- apps/api/app/core/config.py — added sandbox settings: sandbox_enabled=True,
  sandbox_image="python:3.12-slim", sandbox_timeout_seconds=30,
  sandbox_mem_limit="512m", sandbox_cpus=1.0, sandbox_pids_limit=256,
  sandbox_workspace_tmpfs_size="256m", sandbox_max_output_bytes=1_048_576,
  sandbox_workspace_volume="ai-coding-agent_ingest_workspace",
  sandbox_workspace_dir="/var/lib/aca/workspace". Also added GitHub settings:
  github_token="", github_api_base="https://api.github.com".
- apps/api/app/infrastructure/sandbox/policy.py — classify(command) returns
  Classification(verdict, reason) where verdict is "blocked"/"approval"/"allow".
  Block patterns include the fixed rm pattern:
  r"\brm\s+-[a-z]*r[a-z]*f?\s+(/|~|\$HOME)(\*|\s|$)"  (the trailing \b was the
  bug — it failed after /).
- apps/api/app/infrastructure/sandbox/service.py — SandboxService.run_stream(
  command, repo_subpath) spawns hardened container via docker SDK in a thread,
  streams via asyncio.Queue, watchdog timeout. Container kwargs:
  network_mode="none", cap_drop=["ALL"], security_opt=["no-new-privileges"],
  read_only=True, user="1000:1000", tmpfs /workspace + /tmp, mounts workspace
  volume RO at /repo-root then copies repo to tmpfs.
- apps/api/app/domain/sandbox/schemas.py — ClassifyRequest/Response,
  SandboxRunRequest
- apps/api/app/api/v1/sandbox.py — POST /classify, WS /ws (enforces approval
  gate + repo ownership)
- apps/web/src/routes/sandbox.tsx — terminal UI
- apps/api/pyproject.toml — added docker==7.1.0 (Phase 5), openai==1.54.4,
  websockets==13.1 (Phase 4)
- docker-compose.yml — api gets /var/run/docker.sock:/var/run/docker.sock mount
  + SANDBOX_WORKSPACE_VOLUME env; worker has
  PROMETHEUS_MULTIPROC_DIR=/tmp/prometheus_multiproc + expose: 9100

PHASE 6 — GITHUB (completed, verified live):
- apps/api/app/infrastructure/github/client.py — GitHubClient async httpx;
  methods: whoami, get_repo, get_branch_sha, create_branch, get_file_sha,
  put_file, create_pull, get_pull, get_pull_diff, comment_on_issue. Auth header
  Authorization: Bearer <token>.
- apps/api/app/domain/github/service.py — GitHubService.status(), create_pr(req)
  (branch->commit->PR), review_pr(req) (diff->LLM->comment).
  _MAX_DIFF_CHARS=24_000. Uses get_llm_provider().
- apps/api/app/domain/github/schemas.py — GitHubStatus, FileChange,
  CreatePRRequest, CreatePRResponse, ReviewPRRequest (number: int = Field(ge=1)),
  ReviewPRResponse
- apps/api/app/api/v1/github.py — GET /status, POST /pulls, POST /review;
  GitHubError->ConflictError
- apps/web/src/routes/github.tsx — status banner + ReviewPRSection +
  CreatePRSection. Most recent fix in ReviewPRSection:
  const prNumber = Number((number.match(/\d+/) ?? ["0"])[0]); then
  api.githubReviewPR({ owner, repo, number: prNumber, post_comment: post }) and
  guard if (!owner || !repo || prNumber < 1) return;
- apps/web/src/lib/api.ts — added githubStatus, githubCreatePR, githubReviewPR
- apps/web/src/App.tsx + apps/web/src/components/layout/shell.tsx — GitHub route
  + nav (and Sandbox, Memory, Chat, Search nav items added across phases)
- apps/api/app/api/router.py — registers all routers including v1_github,
  v1_sandbox, v1_memory, v1_chat, v1_search, v1_repos
- .env and .env.example — added GITHUB_TOKEN= + GITHUB_API_BASE=
  https://api.github.com

4. ERRORS AND FIXES
--------------------------------------------------------------------------------
- Sandbox "rm -rf /" returned "approval" not "blocked": regex trailing \b fails
  after / (non-word char). Fixed to (/|~|\$HOME)(\*|\s|$). Verified: rm -rf / ->
  blocked, pip install -> approval, ls -> allow.
- Sandbox network test had shell-quoting bug (SyntaxError); re-tested cleanly
  with heredoc -> confirmed NET_BLOCKED OSError (network_mode=none works).
- GitHub "Not Found" (404): user entered owner="Ahammad Shaik" (display name).
  Fix: use login ahammadsk-000. Verified via whoami: login=ahammadsk-000,
  name=Ahammad.Shaik, repo Banking_Project_OOPs-Concept default_branch=main
  public.
- GitHub 403 "Resource not accessible by personal access token": fine-grained
  PAT lacked write perms. Fix: add Contents: Read and write + Pull requests:
  Read and write on the repo. (Editing perms keeps same token value — no
  restart needed.) User fixed it; Create PR then worked (opened PR #1).
- GitHub review HTTP 422: user typed "PR #1" -> Number("PR #1")=NaN->null->fails
  int Field(ge=1). Fixed frontend to extract digits via regex. Backend verified
  working directly (returned a real review catching bugs in the Banking code).
  The fix wasn't in the served bundle initially — required
  rm -rf /app/node_modules/.vite + docker compose restart web (confirmed 3
  matches after).
- Recurring Vite issue: HMR/dep-optimize misses new imports; must clear .vite
  cache + restart web. Recurring throughout.
- Git Bash path mangling: /chart->C:\Program Files\Git\chart and docker socket
  path; resolved by using PowerShell tool or //var/run/docker.sock.
- IDE diagnostics "Cannot find module 'react'/'lucide-react'": FALSE POSITIVES
  — node_modules only exists in container. Consistently ignored.

5. PROBLEM SOLVING
--------------------------------------------------------------------------------
- Completed and verified Phases 1-9 end-to-end. Phase 9 was deployed LIVE to
  Docker Desktop Kubernetes (kind-based; required a local registry at
  localhost:5000 because the kind node can't see host images; api/worker images
  are 9.5GB each). Then cleaned up (helm uninstall, namespace delete, registry
  removed) at user's request since dev continues in docker-compose.
- Phase 6 verified live against user's real repo
  ahammadsk-000/Banking_Project_OOPs-Concept (PR #1 created; AI review backend
  confirmed working).
- Ongoing flagged tech debt: 9.5GB api/worker images (ship full CUDA torch for
  CPU-only); worker metrics gap (closed via multiprocess mode); agent
  shell_exec not yet wired.

6. ALL USER MESSAGES (chronological, recent portion)
--------------------------------------------------------------------------------
- "Please proceed with the Phase 9"
- "i have done this step in the docker desktop (You'd need a cluster. Easiest
  path... enable Docker Desktop's Kubernetes...). Do this what ever neccesary
  like mentioned below [helm install commands]"
- "before moving to next phase, in the below steps do i need to do anything
  from my end, please explain if it is okay please do needfull from your end
  [cleanup steps]"
- "Please proceed with the Phase 5"
- "go with phase 6"
- "when i tyr to create a PR in Ai coding agent ui it is showing below error...
  GitHub: Not Found" (inputs: owner "Ahammad Shaik", repo
  Banking_Project_OOPs-Concept, base main, branch feature/test-pro, etc.)
- "i have changed the owner but got some other different error... GitHub:
  Resource not accessible by personal access token"
- "creating pull request is working correctly, but when i try to AI review a
  pull request it is showing below error... HTTP 422" (entered PR # as "#1")
- "i am still gettiong the below error... HTTP 422" (entered "PR #1")
- "i feel everything is working correct, now it's time to move to the Phase 10"
- Security note from system: .env was modified to add GITHUB_TOKEN=
  github_pat_11AOYZVLQ0... (a real fine-grained PAT — must NOT be echoed or
  exposed; it is server-side only and never sent to the frontend by design).

7. PENDING TASKS
--------------------------------------------------------------------------------
- Phase 10 (Enterprise) — explicitly requested ("move to the Phase 10") but
  scope NOT yet agreed (my scope question was rejected). Full spec: multi-tenant
  orgs+workspaces, fine-grained resource-level RBAC, SSO (OIDC), SCIM, billing
  metering hooks, plugin/tool marketplace.
- Deferred polish items (mentioned, not committed): slim 9.5GB images to ~1-2GB
  (CPU-only torch), wire Phase 5 sandbox into Phase 4 chat as a shell_exec agent
  tool, GitHub App + OAuth + inbound webhooks (need public URL/tunnel + app
  registration).

8. CURRENT WORK
--------------------------------------------------------------------------------
Immediately before this summary, the user said "i feel everything is working
correct, now it's time to move to the Phase 10." I responded by explaining
Phase 10 is the largest phase and partly untestable locally (SSO needs an IdP,
billing needs Stripe, etc.), and I issued an AskUserQuestion proposing 4 scope
options: (1) "Multi-tenancy core (Recommended)" — organizations + memberships +
org-scoped roles owner/admin/member, auto personal-org per user, org-scoped
repositories with role-gated actions, org switcher + Organizations management
UI, defer SSO/SCIM/billing/marketplace; (2) "Lightweight teams only";
(3) "Metering + billing hooks"; (4) "Skip Phase 10, do polish instead". The
user REJECTED this tool use / interrupted, then requested this summary. No
Phase 10 code has been written. No files were created or modified for Phase 10.

9. OPTIONAL NEXT STEP
--------------------------------------------------------------------------------
Await the user's direction on how to proceed with Phase 10, since they
explicitly rejected my scope-selection question. The user's last actionable
request was "now it's time to move to the Phase 10" but they declined the
proposed scoping options. The appropriate next step is to ask — in plain text,
briefly — how they want to slice Phase 10 (or whether they'd prefer a different
approach), rather than assuming a scope and risking a large misdirected build
that touches existing working code. I should NOT begin Phase 10 implementation
until the user clarifies the desired scope, given they just rejected the scope
question.

================================================================================
END OF COMPACT SUMMARY
================================================================================
